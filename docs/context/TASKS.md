# TASKS

## TODO

- add dashboard controls for `ack`, `dismiss`, and `resolve` alert transitions
- add frontend view for `/api/v1/bias`
- replace synthetic ingestion with replayable fixture dataset loader
- add frontend automated test runner
- add persistent operator identity to alert transitions

## IN PROGRESS

- none

## BLOCKED

- none

## DONE

- restructured frontend into `pages`, `hooks`, `services`, `components`, `styles`, `utils`
- moved backend to versioned API under `/api/v1`
- separated backend into `core`, `crud`, `schemas`, `services`
- aligned docs and health-check script to current implementation
